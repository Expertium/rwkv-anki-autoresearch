# FIXC TEACHER DUMP (END-TO-END CONTROL) (2026-08-30). Regenerated because the interval definition changed:
# the old dump's logits were computed on end-to-END inputs and the labels_sum checksum
# cannot see an input-side change. Data-affecting fields MUST match ws.toml exactly.
# KD TEACHER DUMP for the SEED PAIR (2026-08-05) -- the d=128 teacher walking the seed-4321,
# MAX=65536 batch stream that both seed-pair arms will walk. The old dump C:\rwkv_kd_dump\
# t128_iter32 is bound to MAX=32768 + seed 1234 (batch-stream identity is enforced by a per-step
# labels checksum, mismatch = hard exit 43), so this run regenerates it for the new recipe.
#
# EVERY data-affecting field matches the arm tomls (spa_ws.toml / spb_ws.toml): db, user range,
# MAX, fetch count. The seed comes from RWKV_AUGMENT_SEED=4321 in the runner's env. Do not
# "improve" any of these -- drift = exit 43 in the student, or worse, silence.
TRAIN_USERS_START = 1
TRAIN_USERS_END = 5000
VALIDATE_USERS_START = 5001
VALIDATE_USERS_END = 5010

TRAIN_DATASET_LMDB_PATH = "F:/rwkv_lmdb/train_db_5k_h1_fixc"
TRAIN_DATASET_LMDB_SIZE = 400_000_000_000
VALIDATE_DATASET_LMDB_PATH = "F:/rwkv_lmdb/test_db_5k_fixc"
VALIDATE_DATASET_LMDB_SIZE = 250_000_000_000
LABEL_FILTER_LMDB_PATH = "label_filter_db"
LABEL_FILTER_LMDB_SIZE = 40_000_000_000

NUM_FETCH_PROCESSES = 2
MAX_TRAIN_GLOBAL_LEN = 65536

TRAIN_MODE = "WS"
STEP_OFFSET = 1
WARMUP_STEPS = 400
EPOCHS = 1
VALIDATE_EVERY = 1000
PEAK_LR = 1e-3

LOAD_MODEL = false
SAVE_MODEL_FOLDER = "scratchpad/fixc_arm"
SAVE_MODEL_PREFIX = "fixcdump"
DEVICE = "cuda"
DTYPE = "bfloat16"

USE_WANDB = false
WANDB_PROJECT_NAME = "rwkv"
WANDB_RESUME = false
WANDB_RESUME_ID = ""
