# RESEARCH PHASE iter 9: shrink-perturb data-driven init (Andrew's queued idea, 2026-07-02;
# design in research_5k_notes.md "Queued idea -- data-driven initialization", scheme A).
# init = 0.5*champion_final + 0.5*fresh(seed 777) via the new RWKV_INIT_BLEND hook in
# train_rwkv.py (params only; deliberate zeros stay zero, decay ramps identical across draws --
# smoke-verified scratchpad/iter9_sp/blend_smoke.py). Otherwise the EXACT champion recipe:
# champion arch (H=2/K=16, [1,4,3,3,3]), champion HPs, 1-ep WS + 0.25-ep decay, q72u learnable
# cbs, seed 1234. Recycles the champ5k_b1 run's compute into a better starting point under the
# fixed budget (Ash & Adams 2020: keeps solution structure, restores plasticity).
# Gate: >=0.0003 BOTH modes + p<0.0001 both vs champ5k_b1. NOTE margin < ~0.0005 => seed-pair.
TRAIN_USERS_START = 1
TRAIN_USERS_END = 5000
VALIDATE_USERS_START = 5001
VALIDATE_USERS_END = 5010

TRAIN_DATASET_LMDB_PATH = "train_db_5k_h1"
TRAIN_DATASET_LMDB_SIZE = 400_000_000_000
VALIDATE_DATASET_LMDB_PATH = "F:/rwkv_lmdb/test_db_5k"
VALIDATE_DATASET_LMDB_SIZE = 250_000_000_000
LABEL_FILTER_LMDB_PATH = "label_filter_db"
LABEL_FILTER_LMDB_SIZE = 40_000_000_000

NUM_FETCH_PROCESSES = 4
MAX_TRAIN_GLOBAL_LEN = 110000

TRAIN_MODE = "WS"
STEP_OFFSET = 1
WARMUP_STEPS = 200
EPOCHS = 1
VALIDATE_EVERY = 500
PEAK_LR = 1e-3

LOAD_MODEL = false
LOAD_MODEL_FOLDER = "reference"
LOAD_MODEL_NAME = "unused"
SAVE_MODEL_FOLDER = "scratchpad/iter9_sp"
SAVE_MODEL_PREFIX = "iter9spws"
DEVICE = "cuda"
DTYPE = "bfloat16"

USE_WANDB = false
WANDB_PROJECT_NAME = "rwkv"
WANDB_RESUME = false
WANDB_RESUME_ID = ""
