# FEATURE-ABLATION arm abl_batchpos (Andrew 2026-09-03: creation-batch POSITION seems useless). featB's OWN checkpoint, its OWN eval db, 300 users; the only
# variable is RWKV_ABLATE_FEATURES. featB's existing per-user results ARE the control,
# so no control arm is run -- which is also why the user range must match exactly.
FILE_AHEAD = "RWKV-abl_batchpos"
FILE_IMM = "RWKV-P-abl_batchpos"
MODEL_PATH = "scratchpad/features_ab/featB/featB_d_10935.pth"
DEVICE = "cuda"
DTYPE = "bfloat16"
DATASET_LMDB_PATH = "F:/rwkv_lmdb/test_db_5k_id3"
DATASET_LMDB_SIZE = 250_000_000_000
LABEL_FILTER_LMDB_PATH = "F:/rwkv_lmdb/label_filter_db_id"
LABEL_FILTER_LMDB_SIZE = 40_000_000_000
RAW = false
RAW_DB_PATH = "raw/result_db"
RAW_DB_SIZE = 1_000_000_000
USER_START = 5001
USER_END = 5300
NUM_FETCH_PROCESSES = 4
